name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies for TaskService
      run: dotnet restore TaskSystem/TaskService.sln  # Volg het pad naar de .sln

    - name: Restore dependencies for BoardService
      run: dotnet restore BoardService/BoardService.sln  # Volg het pad naar de .sln

    - name: Build TaskService
      run: dotnet build TaskSystem/TaskService.sln --no-restore  # Bouw de oplossing

    - name: Build BoardService
      run: dotnet build BoardService/BoardService.sln --no-restore  # Bouw de oplossing

    - name: Test TaskService
      run: dotnet test TaskSystem/TaskService.sln --no-build --verbosity normal  # Voer tests uit

    - name: Test BoardService
      run: dotnet test BoardService/BoardService.sln --no-build --verbosity normal  # Voer tests uit



